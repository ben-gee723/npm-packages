{
  "name": "npm-packages",
  "version": "1.0.0",
  "description": "npm packaes lessens",
  "scripts": {
    "start": "rm -rf dev-serve && parcel src/index.html --dist-dir dev-serve",
    "build": "rm -rf build && parcel build src/index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "author": "Ben Goldschmidt",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}